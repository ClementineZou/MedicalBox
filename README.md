# MedicalBox 家庭药箱管理系统

一个现代化的智能家庭药箱管理系统，帮助您科学管理家庭药品、设置用药提醒、记录用药历史，并监测健康指标。采用 Material Design 3 设计语言，提供美观直观的用户界面。

## ✨ 核心功能

### 📦 药品管理
- **药品信息录入**：详细记录药品名称、品牌、剂量规格、库存数量、有效期等信息
- **分类管理**：支持15种药品分类（解热镇痛药、抗过敏药、抗感染药、呼吸系统用药等）
- **管控分类标识**：标记处方药、非处方药、精神药品等管控类型，并用不同颜色区分
- **有效期提醒**：自动识别30天内即将过期的药品，用红色高亮提醒
- **存放位置**：记录药品存放位置，方便快速找到
- **搜索与筛选**：支持按药品名称搜索、按分类筛选

### ⏰ 用药提醒
- **灵活的提醒设置**：支持单次、每日、每周、每月等多种提醒频率
- **今日提醒**：首页突出显示当天的用药提醒，避免遗漏
- **即将到来的提醒**：展示未来的用药计划，帮助提前准备
- **剂量信息**：在提醒中显示用药剂量，确保正确服用
- **一键完成**：点击"完成并记录"按钮可选择服用时间（当前时间/提醒时间/自定义时间），自动创建用药记录

### 📊 用药记录
- **详细的用药历史**：记录每次服药的剂量、时间、反应和备注
- **副作用记录**：专门记录用药后的副作用，用红色标记提醒
- **时间筛选**：支持按日期范围筛选记录
- **药品筛选**：可按特定药品查看用药历史
- **编辑与删除**：可随时编辑或删除历史记录

### 💓 健康监测
- **多种生命体征**：支持记录身高、体重、体温、血压、血氧、血糖、心率等7种生命体征
- **趋势分析图表**：通过可视化图表展示健康数据变化趋势
- **参考范围对比**：自动显示正常参考范围，并标注异常数值
- **血压双值记录**：专门支持收缩压/舒张压的记录与分析
- **异常提示**：自动判断数值是否在正常范围内，异常数据用红色标记
- **监测提醒**：可设置定期监测提醒（如每天测血压、每周测体重）
- **日期范围查询**：支持按时间段筛选健康数据

### 👤 用户系统
- **账户注册与登录**：支持邮箱密码注册/登录
- **GitHub 账户关联**：可关联 GitHub 账户进行第三方登录
- **Gravatar 头像**：自动从 Gravatar 获取邮箱对应的头像
- **个人信息管理**：显示账户创建时间、邮箱验证状态等
- **密码修改**：支持修改账户密码（待实现）
- **账户删除**：提供账户删除功能（待实现）

## 🛠 技术栈

### 前端技术
- **Nuxt 3.19.3**：基于 Vue 3 的全栈框架，提供 SSR 和 API 路由
- **Vue 3.5.22**：采用 Composition API，提供响应式用户界面
- **TypeScript**：类型安全的开发体验
- **Tailwind CSS**：实用优先的 CSS 框架
- **Material Design 3**：Google 最新设计语言，提供现代化 UI

### 后端技术
- **Nitro 2.12.7**：Nuxt 的服务器引擎
- **Prisma ORM 5.18.0**：类型安全的数据库访问
- **Better Auth 1.4.0**：现代化的身份验证库
- **SQLite**：轻量级嵌入式数据库（开发环境）

### 第三方集成
- **GitHub OAuth**：第三方账户关联
- **Gravatar**：头像服务集成

## 📁 项目结构

```
MedicalBox/
├── assets/
│   └── css/              # 全局样式和 Tailwind 配置
├── components/           # Vue 组件
│   ├── MedicineModal.vue       # 药品添加/编辑模态框
│   ├── ReminderModal.vue       # 提醒添加/编辑模态框
│   ├── RecordModal.vue         # 用药记录添加/编辑模态框
│   ├── VitalSignModal.vue      # 生命体征记录模态框
│   ├── VitalSignChart.vue      # 健康数据图表组件
│   ├── ConfirmDialog.vue       # 确认对话框组件
│   └── GlobalNotification.vue  # 全局通知组件
├── composables/          # 可组合函数
│   ├── useAuth.ts              # 身份验证状态管理
│   ├── useNotification.ts      # 通知系统
│   ├── useGravatar.ts          # Gravatar 头像
│   └── useFormValidation.ts    # 表单验证
├── layouts/
│   └── default.vue       # 默认布局（包含导航栏）
├── pages/                # 页面路由
│   ├── index.vue               # 首页
│   ├── login.vue               # 登录页
│   ├── register.vue            # 注册页
│   ├── medicines/
│   │   ├── index.vue           # 药品列表
│   │   └── [id]/index.vue      # 药品详情
│   ├── reminders/
│   │   └── index.vue           # 提醒列表
│   ├── records/
│   │   └── index.vue           # 用药记录列表
│   ├── vitals/
│   │   └── index.vue           # 健康监测
│   └── profile/
│       ├── index.vue           # 个人中心
│       └── github-callback.vue # GitHub OAuth 回调
├── server/               # 服务器端代码
│   ├── api/
│   │   ├── auth/               # 身份验证 API
│   │   ├── medicines/          # 药品 CRUD API
│   │   ├── reminders/          # 提醒 CRUD API
│   │   ├── records/            # 用药记录 CRUD API
│   │   ├── vitals/             # 生命体征 CRUD API
│   │   ├── stats/              # 统计数据 API
│   │   └── user/               # 用户信息 API
│   └── utils/
│       ├── auth.ts             # 身份验证工具
│       └── prisma.ts           # Prisma 客户端
├── prisma/
│   ├── schema.prisma     # 数据库模型定义
│   ├── migrations/       # 数据库迁移文件
│   └── seed.ts           # 数据库种子数据
├── types/
│   └── index.ts          # TypeScript 类型定义
├── utils/                # 工具函数
│   ├── dateTime.ts       # 日期时间格式化
│   ├── localTime.ts      # 本地时间处理
│   └── gravatar.ts       # Gravatar 工具
└── plugins/              # Nuxt 插件
    ├── dateTime.ts       # 日期时间插件
    └── notification.ts   # 通知插件
```

## 🗄 数据模型

系统采用 Prisma ORM，包含以下核心数据模型：

### User（用户）
- `id`：用户唯一标识
- `name`：用户姓名
- `email`：邮箱地址（唯一）
- `emailVerified`：邮箱验证状态
- `image`：头像 URL
- `createdAt`：创建时间
- `updatedAt`：更新时间

### Medicine（药品）
- `id`：药品唯一标识
- `userId`：所属用户
- `name`：药品名称
- `brand`：品牌
- `category`：分类（15种）
- `controlTypes`：管控分类
- `dosage`：剂量规格
- `dosageUnit`：剂量单位
- `quantity`：库存数量
- `quantityUnit`：数量单位
- `expiryDate`：有效期
- `location`：存放位置
- `notes`：备注

### Reminder（用药提醒）
- `id`：提醒唯一标识
- `userId`：所属用户
- `medicineId`：关联药品
- `title`：提醒标题
- `description`：描述（JSON格式，包含剂量和说明）
- `reminderTime`：提醒时间
- `frequency`：频率（once/daily/weekly/monthly）
- `isCompleted`：完成状态

### MedicineUsageRecord（用药记录）
- `id`：记录唯一标识
- `userId`：所属用户
- `medicineId`：关联药品
- `dosage`：服用剂量
- `usageTime`：服用时间
- `notes`：备注
- `sideEffectNotes`：副作用记录

### VitalSign（生命体征）
- `id`：记录唯一标识
- `userId`：所属用户
- `type`：类型（height/weight/temperature/bloodPressure/bloodOxygen/bloodGlucose/heartRate）
- `value`：数值
- `systolic`：收缩压（血压专用）
- `diastolic`：舒张压（血压专用）
- `unit`：单位
- `measureTime`：测量时间
- `isNormal`：是否正常
- `notes`：备注

### VitalSignReminder（生命体征提醒）
- `id`：提醒唯一标识
- `userId`：所属用户
- `vitalSignType`：生命体征类型
- `title`：提醒标题
- `reminderTime`：提醒时间
- `frequency`：频率

### VitalSignReferenceRange（参考范围）
- `id`：范围唯一标识
- `type`：生命体征类型
- `minValue`：最小值
- `maxValue`：最大值
- `unit`：单位

## 🚀 快速开始

### 环境要求
- **Node.js** 18.x 或更高版本
- **NPM** 8.x 或更高版本
- 现代浏览器（Chrome、Firefox、Safari、Edge）

### 安装步骤

1. **克隆项目**
```bash
git clone https://github.com/ClementineZou/MedicalBox.git
cd MedicalBox
```

2. **安装依赖**
```bash
npm install
```

3. **配置环境变量**

创建 `.env` 文件（参考 `.env.example`）：
```env
# Better Auth 密钥（必需）
BETTER_AUTH_SECRET=your-secret-key-here

# GitHub OAuth（可选）
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# 数据库 URL（开发环境使用 SQLite）
DATABASE_URL="file:./prisma/dev.db"
```

4. **初始化数据库**
```bash
# 应用数据库迁移
npx prisma migrate dev

# 生成 Prisma 客户端
npx prisma generate

# （可选）填充示例数据
npx prisma db seed
```

5. **启动开发服务器**
```bash
npm run dev
```

访问 http://localhost:3000 查看应用

### 生产部署

```bash
# 构建生产版本
npm run build

# 启动生产服务器
npm run start
```

## 📝 使用说明

### 首次使用
1. 访问注册页面创建账户
2. 登录后进入药品管理页面
3. 添加家中的药品信息
4. 设置用药提醒
5. 记录每次用药情况
6. 定期记录健康数据

### GitHub 关联
1. 进入个人中心
2. 点击"关联"GitHub 按钮
3. 授权后自动关联账户

### Gravatar 头像
系统自动根据邮箱从 Gravatar 获取头像，在 [Gravatar.com](https://gravatar.com) 注册邮箱即可显示自定义头像。

## 🔧 开发指南

### 添加新的 API 端点
在 `server/api/` 目录下创建新的路由文件，例如：
```typescript
// server/api/example.get.ts
export default defineEventHandler(async (event) => {
  return { message: 'Hello World' }
})
```

### 添加新页面
在 `pages/` 目录下创建 Vue 文件，Nuxt 会自动生成路由。

### 数据库迁移
修改 `prisma/schema.prisma` 后，运行：
```bash
npx prisma migrate dev --name migration_name
```

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 👨‍💻 作者

**Clementine Zou**
- GitHub: [@ClementineZou](https://github.com/ClementineZou)

## 🙏 致谢

- [Nuxt.js](https://nuxt.com/)
- [Vue.js](https://vuejs.org/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Prisma](https://www.prisma.io/)
- [Better Auth](https://www.better-auth.com/)
- [Material Design 3](https://m3.material.io/)